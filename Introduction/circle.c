#include<stdio.h>
#include<stdlib.h>
int main()
{
	int r=6;
	float pi=3.14,per,area;
	per=2*pi*r;
	area=pi*r*r;
	printf("Perimeter Of The Circle =%f ",per);
    printf("\nArea Of The Circle =%f ",area);	
    return 0;
}
