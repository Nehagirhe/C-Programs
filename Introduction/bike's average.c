#include <stdio.h>
#include <stdlib.h>
/* run this program using the console pauser or add your own getch, system("pause") or input loop */
int main(int argc, char *argv[]) {
int dist=350,fuel=5;
float avg;avg=dist/fuel;
printf("Average Consumption %f (km/lt)\n",avg);
	return 0;
}
