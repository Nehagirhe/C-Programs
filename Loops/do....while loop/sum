#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
	int num,x,sum=0;
	printf("Enter Numbers:");
	scanf("%d",&num);             
	do
	{
		x=num%10;             
		sum=sum+x;            
    num=num/10;          
	}
	while(num>0);                    
	printf("sum of digits of the number = %d",sum);        
	return 0;
}
