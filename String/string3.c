#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	char a[13];//="w3resource";
	int i;
	//int n=sizeofa[]/a(10);
	printf("the character of the strings are:");
		for(i=13;i>0;i--)
	{
		
		scanf("%c",&a[i]);
		printf("%c ",a[i]);
	}
	strrev(str); 
	printf("%s ",str);
	return 0;
}
